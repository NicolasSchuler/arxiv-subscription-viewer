name: CI/CD

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.13

      - name: Install dependencies
        run: uv sync

      - name: Lint
        run: uv run ruff check .

      - name: Format check
        run: uv run ruff format --check .

      - name: Type check
        run: uv run pyright

      - name: Tests with coverage
        run: uv run pytest --cov --cov-report=term-missing

  quality:
    needs: ci
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.13

      - name: Install dependencies
        run: uv sync

      - name: Complexity gate
        run: uv run xenon src/arxiv_browser/ --max-absolute C --max-modules C --max-average B

      - name: Security scan
        run: uv run bandit -c pyproject.toml -r src/arxiv_browser

      - name: Dead code check
        run: uv run vulture src/arxiv_browser vulture_whitelist.py --min-confidence 80

      - name: Dependency hygiene
        run: uv run deptry .

      - name: App size guardrail
        run: |
          APP_LINES=$(wc -l < src/arxiv_browser/app.py)
          echo "src/arxiv_browser/app.py lines: ${APP_LINES}"
          test "${APP_LINES}" -le 5000

  publish:
    needs: [ci, quality]
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true

      - name: Set up Python
        run: uv python install 3.13

      - name: Build package
        run: uv build

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
